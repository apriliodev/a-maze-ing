[tool.poetry]
name = "A-Maze-Ing"
version = "0.1.0"
description = "Maze Generator"
authors = ["bdecourt", "mchapuis"]


[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"